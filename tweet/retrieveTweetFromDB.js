// Get the tweets from the DB
// Also log/check how many tweets remain
// if below a certain number run generateNewTweets

export function retrieveTweetFromDB() {
  // go to DB and get tweet + tweets length
}
